{% extends 'page.html.twig' %}
{% block title %}Vehicule{% endblock %}

{% block body %}
    <h1>Vehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicule.id }}</td>
            </tr>
            <tr>
                <th>Immatriculation</th>
                <td>{{ vehicule.immatriculation }}</td>
            </tr>
            <tr>
                <th>DateImmatriculation</th>
                <td>{{ vehicule.DateImmatriculation ? vehicule.DateImmatriculation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NumeroSerie</th>
                <td>{{ vehicule.numeroSerie }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ vehicule.kilometrage }}</td>
            </tr>
            <tr>
                <th>DateKilometrage</th>
                <td>{{ vehicule.dateKilometrage ? vehicule.dateKilometrage|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EntreeParc</th>
                <td>{{ vehicule.entreeParc ? vehicule.entreeParc|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>SortieParc</th>
                <td>{{ vehicule.sortieParc ? vehicule.sortieParc|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>
                {{ vehicule.statut }}
                </td>
            </tr>
            <tr>
                <th>Réservation</th>
                <td>
                {# link to booking details #}
                {# for each booking #}

                {% for booking in vehicule.bookings %}
                    <a href="{{ path('app_booking_show', {'id': booking.id}) }}">{{ booking.id }}</a>
                {% endfor %}


                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vehicule_index') }}">back to list</a>

    <a href="{{ path('app_vehicule_edit', {'id': vehicule.id}) }}">edit</a>

    {{ include('vehicule/_delete_form.html.twig') }}
{% endblock %}
